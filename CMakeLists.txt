cmake_minimum_required(VERSION 3.24)
project(laya VERSION 0.2.0 LANGUAGES C CXX)
set(CMAKE_RUNTIME_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/bin)
option(LAYA_CUDA "Build the NVIDIA CUDA inference backend" ON)
option(LAYA_VULKAN "Build the Vulkan inference backend" OFF)
set(GGML_VULKAN ${LAYA_VULKAN} CACHE BOOL "" FORCE)
set(GGML_CUDA ${LAYA_CUDA} CACHE BOOL "" FORCE)
set(GGML_CUDA_GRAPHS ON CACHE BOOL "" FORCE)
set(GGML_BUILD_TESTS OFF CACHE BOOL "" FORCE)
set(GGML_BUILD_EXAMPLES OFF CACHE BOOL "" FORCE)
add_subdirectory(third_party/ggml)
if(LAYA_VULKAN)
  include(cmake/VulkanPrecision.cmake)
endif()
if(LAYA_CUDA)
  enable_language(CUDA)
  find_package(CUDAToolkit REQUIRED)
  # Extend one CUDA dispatcher without editing the pinned dependency checkout.
  set_property(SOURCE "${CMAKE_CURRENT_SOURCE_DIR}/third_party/ggml/src/ggml-cuda/fattn.cu"
    TARGET_DIRECTORY ggml-cuda APPEND PROPERTY COMPILE_DEFINITIONS
    ggml_cuda_flash_attn_ext=ggml_cuda_flash_attn_ext_upstream)
  # Preserve FP32 accumulators and output for half-input cuBLAS products.
  set(cuda_source "${CMAKE_CURRENT_SOURCE_DIR}/third_party/ggml/src/ggml-cuda/ggml-cuda.cu")
  file(READ "${cuda_source}" cuda_code)
  set(old_policy "prefer_f32_output = cc == GGML_CUDA_CC_VOLTA || GGML_CUDA_CC_IS_RDNA4(cc) || GGML_CUDA_CC_IS_CDNA(cc);")
  string(FIND "${cuda_code}" "${old_policy}" policy_position)
  if(policy_position EQUAL -1)
    message(FATAL_ERROR "Pinned CUDA accumulation policy changed")
  endif()
  string(REPLACE "${old_policy}" "prefer_f32_output = true;" cuda_code "${cuda_code}")
  string(REPLACE "static bool ggml_cuda_compute_forward("
    "bool laya_cuda_custom(ggml_backend_cuda_context&, ggml_tensor*);\nstatic bool ggml_cuda_compute_forward("
    cuda_code "${cuda_code}")
  string(REPLACE "        case GGML_OP_CONCAT:"
    "        case GGML_OP_CUSTOM: return laya_cuda_custom(ctx, dst);\n        case GGML_OP_CONCAT:"
    cuda_code "${cuda_code}")
  # The capability query has different argument names and must not execute kernels.
  string(REPLACE "case GGML_OP_CUSTOM: return laya_cuda_custom(ctx, dst);\n        case GGML_OP_CONCAT:\n            {\n                ggml_type src0_type = op->src[0]->type;"
    "case GGML_OP_CUSTOM: return strcmp(op->name, \"laya.split-f16\") == 0 || strcmp(op->name, \"laya.linear-bf16\") == 0 || strcmp(op->name, \"laya.gelu-bf16\") == 0 || strcmp(op->name, \"laya.mlp-bf16\") == 0 || strcmp(op->name, \"laya.norm-bf16\") == 0 || strcmp(op->name, \"laya.merge-f16\") == 0 || strcmp(op->name, \"laya.pack-qkv-bf16\") == 0 || strcmp(op->name, \"laya.pack-qkv\") == 0 || strcmp(op->name, \"laya.mlp-split\") == 0 || strcmp(op->name, \"laya.mask-local\") == 0 || strcmp(op->name, \"laya.mask-global\") == 0;\n        case GGML_OP_CONCAT:\n            {\n                ggml_type src0_type = op->src[0]->type;"
    cuda_code "${cuda_code}")
  file(WRITE "${CMAKE_CURRENT_BINARY_DIR}/laya-ggml-cuda.cu" "${cuda_code}")
  get_target_property(cuda_sources ggml-cuda SOURCES)
  list(TRANSFORM cuda_sources REPLACE "^(.*/)?ggml-cuda[.]cu$" "${CMAKE_CURRENT_BINARY_DIR}/laya-ggml-cuda.cu")
  set_property(TARGET ggml-cuda PROPERTY SOURCES "${cuda_sources}")
  # BF16 operator boundaries depend on strict device math. Keep this target
  # separate from the dependency's --use_fast_math compilation flags.
  add_library(laya-cuda-bf16 OBJECT src/bf16.cu)
  get_target_property(bf16_architectures ggml-cuda CUDA_ARCHITECTURES)
  list(FILTER bf16_architectures EXCLUDE REGEX "^[567][0-9]")
  if(NOT bf16_architectures)
    set(bf16_architectures 80-virtual)
  endif()
  set_target_properties(laya-cuda-bf16 PROPERTIES POSITION_INDEPENDENT_CODE ON CUDA_ARCHITECTURES "${bf16_architectures}")
  target_compile_definitions(laya-cuda-bf16 PRIVATE $<TARGET_PROPERTY:ggml-cuda,COMPILE_DEFINITIONS>)
  target_include_directories(laya-cuda-bf16 PRIVATE $<TARGET_PROPERTY:ggml-cuda,INCLUDE_DIRECTORIES>
    "${CMAKE_CURRENT_SOURCE_DIR}/third_party/ggml/src/ggml-cuda")
  target_link_libraries(laya-cuda-bf16 PRIVATE CUDA::cudart CUDA::cublas CUDA::cublasLt)
  target_sources(ggml-cuda PRIVATE $<TARGET_OBJECTS:laya-cuda-bf16>)
  target_link_libraries(ggml-cuda PRIVATE CUDA::cublasLt)
  # Keep the selected math libraries ahead of another toolkit on the system.
  foreach(library CUDA::cublas CUDA::cublasLt)
    get_target_property(library_path ${library} IMPORTED_LOCATION)
    get_filename_component(library_directory "${library_path}" DIRECTORY)
    set_property(TARGET ggml-cuda APPEND PROPERTY BUILD_RPATH "${library_directory}")
  endforeach()
  target_sources(ggml-cuda PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/attention.cu" "${CMAKE_CURRENT_SOURCE_DIR}/src/precision.cu")
  target_include_directories(ggml-cuda PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/third_party/ggml/src/ggml-cuda")
endif()
find_package(nlohmann_json 3.11 REQUIRED)
find_package(ICU REQUIRED COMPONENTS uc i18n)
add_library(laya src/decision.cpp src/precision.cpp src/tokenizer.cpp src/runtime.cpp src/protocol.cpp)
target_include_directories(laya PUBLIC include)
target_compile_features(laya PUBLIC cxx_std_20)
target_compile_options(laya PRIVATE -Wall -Wextra -Wpedantic)
target_link_libraries(laya PUBLIC ggml nlohmann_json::nlohmann_json ICU::uc ICU::i18n)
if(LAYA_CUDA)
  target_compile_definitions(laya PRIVATE LAYA_CUDA=1)
  target_link_libraries(laya PRIVATE ggml-cuda)
endif()
if(LAYA_VULKAN)
  target_compile_definitions(laya PRIVATE LAYA_VULKAN=1)
  target_link_libraries(laya PRIVATE ggml-vulkan)
endif()
add_executable(laya-cli src/main.cpp)
find_package(Threads REQUIRED)
add_library(laya-http STATIC src/http.cpp)
target_include_directories(laya-http PUBLIC include PRIVATE third_party/cpp-httplib)
target_compile_features(laya-http PUBLIC cxx_std_20)
target_compile_options(laya-http PRIVATE -Wall -Wextra -Wpedantic)
target_link_libraries(laya-http PUBLIC nlohmann_json::nlohmann_json Threads::Threads)
target_link_libraries(laya-cli PRIVATE laya laya-http)
include(CTest)
if(BUILD_TESTING)
  if(LAYA_VULKAN)
    add_executable(test-vulkan-spirv tests/vulkan_spirv.cpp)
    target_include_directories(test-vulkan-spirv PRIVATE src)
    add_test(NAME vulkan-spirv COMMAND test-vulkan-spirv)
    add_executable(test-vulkan-bf16-range tests/vulkan_bf16_range.cpp)
    target_include_directories(test-vulkan-bf16-range PRIVATE src)
    target_link_libraries(test-vulkan-bf16-range PRIVATE ggml-base)
    add_test(NAME vulkan-bf16-range COMMAND test-vulkan-bf16-range)
    add_executable(test-vulkan-bf16-range-gpu tests/vulkan_bf16_range_gpu.cpp)
    target_link_libraries(test-vulkan-bf16-range-gpu PRIVATE ggml ggml-vulkan)
    add_test(NAME vulkan-bf16-range-gpu COMMAND test-vulkan-bf16-range-gpu)
    set_tests_properties(vulkan-bf16-range-gpu PROPERTIES SKIP_RETURN_CODE 77)
    add_executable(test-vulkan-bf16-projection tests/vulkan_projection.cpp)
    target_compile_definitions(test-vulkan-bf16-projection PRIVATE LAYA_TEST_BF16=1)
    target_link_libraries(test-vulkan-bf16-projection PRIVATE ggml ggml-vulkan)
    add_test(NAME vulkan-bf16-projection COMMAND test-vulkan-bf16-projection)
    set_tests_properties(vulkan-bf16-projection PROPERTIES SKIP_RETURN_CODE 77)
    add_executable(test-vulkan-projection tests/vulkan_projection.cpp)
    target_link_libraries(test-vulkan-projection PRIVATE ggml ggml-vulkan)
    add_test(NAME vulkan-projection COMMAND test-vulkan-projection)
    set_tests_properties(vulkan-projection PROPERTIES SKIP_RETURN_CODE 77)
    add_executable(test-vulkan-attention tests/vulkan_attention.cpp)
    target_link_libraries(test-vulkan-attention PRIVATE ggml ggml-vulkan)
    add_test(NAME vulkan-attention COMMAND test-vulkan-attention)
    set_tests_properties(vulkan-attention PROPERTIES SKIP_RETURN_CODE 77)
    add_executable(test-vulkan-softmax tests/vulkan_softmax.cpp)
    target_link_libraries(test-vulkan-softmax PRIVATE ggml ggml-vulkan)
    add_test(NAME vulkan-softmax COMMAND test-vulkan-softmax)
    set_tests_properties(vulkan-softmax PROPERTIES SKIP_RETURN_CODE 77)
    add_executable(test-vulkan-precision tests/vulkan_precision.cpp)
    target_link_libraries(test-vulkan-precision PRIVATE ggml ggml-vulkan)
    target_include_directories(test-vulkan-precision PRIVATE src)
    add_test(NAME vulkan-precision COMMAND test-vulkan-precision)
    set_tests_properties(vulkan-precision PROPERTIES SKIP_RETURN_CODE 77)
  endif()
  add_executable(test-http tests/http.cpp)
  target_include_directories(test-http PRIVATE third_party/cpp-httplib)
  target_link_libraries(test-http PRIVATE laya-http)
  add_test(NAME http COMMAND test-http)
  set_tests_properties(http PROPERTIES TIMEOUT 30)
  add_executable(test-decision tests/decision.cpp)
  target_link_libraries(test-decision PRIVATE laya)
  add_test(NAME decision COMMAND test-decision)
  add_executable(test-tokenizer tests/tokenizer.cpp)
  target_link_libraries(test-tokenizer PRIVATE laya)
  if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/models/laya/tokenizer/tokenizer.json")
    add_test(NAME tokenizer COMMAND test-tokenizer
      "${CMAKE_CURRENT_SOURCE_DIR}/models/laya/tokenizer/tokenizer.json"
      "${CMAKE_CURRENT_SOURCE_DIR}/tests/tokenizer-cases.json")
  endif()
  foreach(variant multilingual typed-decisions)
    if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/models/laya/${variant}/tokenizer/tokenizer.json")
      if(variant STREQUAL "multilingual")
        set(fixtures tokenizer-multilingual.json)
      else()
        set(fixtures tokenizer-cases.json)
      endif()
      add_test(NAME tokenizer-${variant} COMMAND test-tokenizer
        "${CMAKE_CURRENT_SOURCE_DIR}/models/laya/${variant}/tokenizer/tokenizer.json"
        "${CMAKE_CURRENT_SOURCE_DIR}/tests/${fixtures}")
    endif()
  endforeach()
  if(LAYA_CUDA AND LAYA_VULKAN)
    add_executable(test-backend-replay tests/backend_replay.cpp)
    target_link_libraries(test-backend-replay PRIVATE laya)
    foreach(variant english multilingual typed-decisions)
      set(checkpoint "${CMAKE_CURRENT_SOURCE_DIR}/models/laya")
      if(NOT variant STREQUAL "english")
        string(APPEND checkpoint "/${variant}")
      endif()
      if(EXISTS "${checkpoint}/model.safetensors")
        add_test(NAME backend-replay-${variant} COMMAND test-backend-replay "${checkpoint}")
        set_tests_properties(backend-replay-${variant} PROPERTIES TIMEOUT 120 RUN_SERIAL TRUE)
      endif()
    endforeach()
  endif()
  if(LAYA_CUDA)
    add_executable(test-bf16 tests/bf16.cpp)
    target_link_libraries(test-bf16 PRIVATE laya ggml-cuda)
    add_test(NAME bf16 COMMAND test-bf16)
    set_tests_properties(bf16 PROPERTIES SKIP_RETURN_CODE 77)
    add_executable(test-attention tests/attention.cpp)
    target_link_libraries(test-attention PRIVATE ggml ggml-cuda)
    add_test(NAME attention COMMAND test-attention)
    add_executable(test-fused tests/fused.cpp)
    target_link_libraries(test-fused PRIVATE laya ggml-cuda)
    add_test(NAME fused-operations COMMAND test-fused)
    add_executable(test-matmul tests/matmul.cpp)
    target_link_libraries(test-matmul PRIVATE laya ggml-cuda)
    add_test(NAME compensated-matmul COMMAND test-matmul)
  endif()
endif()
